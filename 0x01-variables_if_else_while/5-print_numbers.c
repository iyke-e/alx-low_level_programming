#include <stdio.h>
/**
 * main -main block
 * Description: printing Numbers
 * Return: Always 0
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%i", num);
	printf("\n");
		return (0);
}
